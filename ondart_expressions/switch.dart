main() {
  var action = 'move';
  switch (action) {
    case 'down':
      print(action);
      break;
    case 'move':
      print(action);
      break;
    default:
      print('unknown action');
  }
}

