main() {
  var action = 'move';
  switch (action) {
    case 'down':
      print(action);
      break; // put it in a comment
    case 'move':
      print(action);
      break; // put it in a comment
    default:
      print('unknown action');
  }
}

