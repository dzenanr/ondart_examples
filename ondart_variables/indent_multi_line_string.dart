main() {
  var s2 = '''
    Use a triple quote, with either single quote or double quote.
    Note the initial newline is ignored, but the last newline is not ignored.
  ''';

  print('s2:');
  print(s2);
}

